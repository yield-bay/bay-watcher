query PDDs($pa: String!) {
  pairDayDatas(
    orderDirection: desc
    orderBy: date
    first: 7
    where: { pairAddress: $pa }
  ) {
    date
    dailyVolumeUSD
    pairAddress
    id
    token0 {
      symbol
    }
    token1 {
      symbol
    }
  }
}
